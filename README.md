# Jest is the testing framework mainly used for unit testing.

To create package.json file.
  - npm init -y
  
To change:
```
"scripts": {
    "test": "jest --coverage"
  }
  
```

To run the .test.js file.
  - npm test
